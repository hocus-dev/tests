image:
  file: hocus.Dockerfile
  buildContext: ./
tasks:
  - name: inner hello world
    prebuild: echo "inner init" > init.txt
    workspace: echo "command" > command.txt
  - name: inner second task
    prebuild: echo "inner init2" > init2.txt
    workspace: echo "command2" > command2.txt
