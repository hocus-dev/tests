image:
  file: hocus.Dockerfile
  buildContext: ../resources
tasks:
  - name: inner hello world
    init: echo "inner init" > init.txt
    command: echo "command" > command.txt
  - name: inner second task
    init: echo "inner init2" > init2.txt
    command: echo "command2" > command2.txt
