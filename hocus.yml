image:
  file: hocus.Dockerfile
  buildContext: ./
tasks:
  - name: sleep1
    prebuild: sleep 999999
    workspace: echo "workspace" > workspace.txt
  - name: sleep2
    prebuild: sleep 999999
    workspace: echo "workspace2" > workspace2.txt
